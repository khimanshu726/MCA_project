services:
  - type: web
    name: elite-empressions
    runtime: node
    plan: free
    buildCommand: npm install --include=dev && npm run build && npm run build:admin
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
